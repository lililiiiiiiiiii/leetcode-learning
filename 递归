
删除链表的第N个节点（不是很懂递归咋回事啊，要加强！！！！）
class Solution:
    def removeNthFromEnd(self, head: ListNode, n: int) -> ListNode:
        global count
        if head is None:
            count=0
            return None
        head.next=self.removeNthFromEnd(head.next,n)#将指针调向最后
        count+=1
        return head.next if n==count else head 
        
 #只要没有指针指向第n个数就可以去除第n个数 


